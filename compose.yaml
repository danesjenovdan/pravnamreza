volumes:
  postgres-data:


services:
  db:
    image: postgres:16-alpine
    volumes:
      - postgres-data:/var/lib/postgresql/data
    expose:
      - "5432"
    environment:
      POSTGRES_DB: pravna_mreza
      POSTGRES_USER: pravna_mreza
      POSTGRES_PASSWORD: changeme

  wagtail:
    build: ./pravna-mreza.si
    command: ./manage.py runserver 0.0.0.0:8000
    volumes:
      - ./pravna-mreza.si:/app:delegated,rw
    ports:
      - "8000:8000"
    environment:
      DJANGO_RUNSERVER_HIDE_WARNING: "true"
      DJANGO_DATABASE_HOST: db
      DJANGO_DATABASE_NAME: pravna_mreza
      DJANGO_DATABASE_USERNAME: pravna_mreza
      DJANGO_DATABASE_PASSWORD: changeme
    depends_on:
      - db
